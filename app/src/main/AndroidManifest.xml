<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gerenciamento_soii">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Gerenciamento_SOII">
        <activity android:name=".MainActivity5_instrucoes"></activity>
        <activity android:name=".MainActivity2_TAB_PROCESSO" />
        <activity android:name=".MainActivity4creditos" />
        <activity android:name=".MainActivity3_TABpagSO" />
        <activity android:name=".MainActivity2Menu" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>